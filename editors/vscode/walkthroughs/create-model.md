# Create your first model

Run **Rossi: New Event-B Project** to scaffold a project folder containing:

- a starter `.eventb` file with a small **context** and **machine**,
- a getting-started `README.md`,
- a `.gitignore`.

The new folder opens automatically so you can start editing right away.
